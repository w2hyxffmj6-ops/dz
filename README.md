# Светофор с графическим интерфейсом

Простая программа, имитирующая работу светофора с графическим интерфейсом на Python.

## Установка и запуск

1. Убедитесь, что у вас установлен Python 3.x
2. Скопируйте код программы в файл `traffic_light.py`
3. Запустите программу командой:

```bash
python traffic_light.py
```

## Описание программы

Программа создает графическое окно с визуализацией светофора и элементами управления:

```
┌─────────────────────────┐
│       СВЕТОФОР          │
├─────────────────────────┤
│                         │
│        ████████         │
│        █ ● красный █    │  Красный - СТОП
│        █ ○ желтый  █    │  Желтый - ВНИМАНИЕ
│        █ ○ зеленый █    │  Зеленый - ИДТИ
│        ████████         │
│                         │
├─────────────────────────┤
│ [ Старт ] [ Стоп ]      │
│                         │
│ [ Ручное управление ]   │
│                         │
│ Светофор выключен       │
└─────────────────────────┘
```

## Возможности программы

### 1. Автоматический режим
- Нажмите кнопку **"Старт"** для запуска автоматической работы
- Светофор переключается в последовательности:
  - 🔴 Красный: 5 секунд
  - 🟡 Желтый: 2 секунды
  - 🟢 Зеленый: 5 секунд
  - 🟡 Желтый: 2 секунды
  - (цикл повторяется)

### 2. Ручное управление
- Нажмите кнопку **"Ручное управление"**
- Появятся три кнопки для управления цветами:
  - **"Красный"** - включить красный сигнал
  - **"Желтый"** - включить желтый сигнал
  - **"Зеленый"** - включить зеленый сигнал

### 3. Остановка
- Нажмите кнопку **"Стоп"** для остановки автоматического режима

## Структура кода

Программа состоит из одного класса `TrafficLight` с методами:

- `__init__()` - инициализация интерфейса
- `set_light()` - установка определенного цвета
- `start_traffic_light()` - запуск автоматического режима
- `stop_traffic_light()` - остановка светофора
- `traffic_light_cycle()` - цикл автоматической работы
- `toggle_manual()` - переключение между режимами

## Тайминги работы

В автоматическом режиме светофор работает со следующими интервалами:

```
Красный сигнал:   5 секунд
Желтый сигнал:    2 секунды
Зеленый сигнал:   5 секунды
Желтый сигнал:    2 секунды
```

## Пример использования кода

```python
# Создание экземпляра светофора
root = tk.Tk()
app = TrafficLight(root)
root.mainloop()
```

<img width="190" height="475" alt="image" src="https://github.com/user-attachments/assets/c101ba28-fad8-46cb-9131-9fa42ebd7f57" />


## Особенности реализации

1. Графический интерфейс создан с помощью библиотеки tkinter
2. Для работы в автоматическом режиме используется отдельный поток
3. Все элементы управления интуитивно понятны
4. Программа не требует установки дополнительных библиотек
5. Интерфейс адаптирован для разных операционных систем

## Зависимости

Программа использует только стандартные библиотеки Python:
- `tkinter` - для графического интерфейса
- `threading` - для многопоточности
- `time` - для управления временными интервалами

## Технические детали

- Размер окна: 200x450 пикселей
- Окно не изменяет размер (фиксированное)
- Цвета сигналов: красный (#FF0000), желтый (#FFFF00), зеленый (#00FF00)
- Фон программы: серый (#808080)

## Совместимость

Программа работает на:
- Windows 7/8/10/11
- macOS
- Linux (с установленным Python и tkinter)

## Лицензия

Программа распространяется свободно. Может быть использована в образовательных целях, для демонстрации работы светофора или как основа для более сложных проектов.

## Поддержка

Для работы программы требуется Python версии 3.6 или выше. Если программа не запускается, проверьте установку Python и наличие библиотеки tkinter.

## Дальнейшее развитие

Программа может быть улучшена добавлением:
- Настройки времени для каждого сигнала
- Звуковых сигналов
- Анимации переключения
- Нескольких светофоров для перекрестка
- Режима "ночной" работы (мигающий желтый)
